import { SearchBookListPipe } from './search-book-list.pipe';

describe('SearchBookListPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBookListPipe();
    expect(pipe).toBeTruthy();
  });
});
