import { EmployeeSerachPipe } from './employee-serach.pipe';

describe('EmployeeSerachPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeSerachPipe();
    expect(pipe).toBeTruthy();
  });
});
