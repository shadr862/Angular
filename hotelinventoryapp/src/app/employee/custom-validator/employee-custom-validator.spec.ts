import { EmployeeCustomValidator } from './employee-custom-validator';

describe('EmployeeCustomValidator', () => {
  it('should create an instance', () => {
    expect(new EmployeeCustomValidator()).toBeTruthy();
  });
});
